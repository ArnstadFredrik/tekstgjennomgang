# Tekstgjennomgang podcast prep

## Project path
Defaults to `$HOME/tekstgjennomgang`.  
Can be changed with `-p --path` flag or `TEKSTGJENNOMGANG_PATH` env variable.  
`export TEKSTGJENNOMGANG_PATH="$HOME/projects"`
